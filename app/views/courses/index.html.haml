%h2 Search the database for a course
= form_tag :action => 'search_course' do
  = label_tag 'search_terms', 'Search Terms'
  = text_field_tag 'search_terms'
  = submit_tag "Search Course"

%h1 All Courses

%table#courses
  %thead
    %tr
      %th Course Title
      %th Code
      %th Professor
  %tbody
    - @courses.each do |course|
      %tr
        %td= link_to "#{course.title}", users_path
        %td= "#{course.coursecode}"
        %td= "#{course.coursecode}"
        
= link_to 'Add new course', new_course_path