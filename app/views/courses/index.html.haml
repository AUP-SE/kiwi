%h1 All Courses

%table#courses
  %thead
    %tr
      %th Course Title
      %th Code
      %th Professor
  %tbody
    - @courses.each do |course|
      %tr
        %td= link_to "#{course.title}", users_path
        %td= "#{course.coursecode}"
        %td= "#{course.coursecode}"
        
= link_to 'Add new course', new_course_path