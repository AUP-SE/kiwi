<h1> All Courses:</h1>
<!-- = render "welcome/_index" -->
<table>#Courses
  <% @courses.each do |course| %>
    <tr>
    <td><%= course.title %></td>
    <td><%= course.coursecode %></td>
    <td><%= course.professor %></td>
    </tr>
<% end %>

</table>
<br>